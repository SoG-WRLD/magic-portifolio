export default function Loading() {
  return (
    <div className="flex items-center justify-center h-screen">
      <span className="text-xl text-primary-400 animate-pulse">Loading projects...</span>
    </div>
  );
}
